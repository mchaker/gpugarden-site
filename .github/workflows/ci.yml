name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.18.1

      - name: Setup pnpm (pin exact)
        uses: pnpm/action-setup@v4
        with:
          # pin pnpm to the exact version required by the repo
          version: 10.18.3

      - name: Verify pnpm is available
        run: pnpm -v

      - name: Install dependencies
        run: pnpm install --frozen-lockfile=false

      - name: Verify
        run: pnpm ci:verify

      - name: Build
        run: pnpm build

**Prerequisites:**  Node.js >=20.9.0

1. Install dependencies:
   `npm install`
2. Dev environment:
   `npm run dev`
3. Build:
   `npm run build`
4. Launch production build:
   `npm run start`

Only weird thing in this codebase is the toml stuff handled by `lib/services.ts`.
